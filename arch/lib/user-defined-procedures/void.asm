
VOID_BODY:
	PUSH(FP);
	MOV(FP,SP);

	CMP(FPARG(1), IMM(0));
	JUMP_EQ(GOT_ENOUGH_ARGS_VOID);
	PUSH(FPARG(1));
	PUSH(IMM(0));
	CALL(THROW_WRONG_NUMBER_OF_ARGS);

GOT_ENOUGH_ARGS_VOID:
	
	MOV(R0, IMM(1));

	MOV(SP,FP);
	POP(FP);
	RETURN;