
CONS_BODY:
	PUSH(FP);
	MOV(FP,SP);

	CMP(FPARG(1), IMM(2));
	JUMP_EQ(GOT_ENOUGH_ARGS_CONS);
	PUSH(FPARG(1));
	PUSH(IMM(1));
	CALL(THROW_WRONG_NUMBER_OF_ARGS);

GOT_ENOUGH_ARGS_CONS:
	
	PUSH(FPARG(3));
	PUSH(FPARG(2));
	CALL(MAKE_SOB_PAIR);
	DROP(2);

	MOV(SP,FP);
	POP(FP);
	RETURN;